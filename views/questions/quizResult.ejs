<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Quiz Result</title>
  <!-- Include any CSS stylesheets -->
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
 <%- include('../layouts/mainLayout') %>
  <h1>Quiz Result</h1>
  
  <% if (score !== undefined && totalQuestions !== undefined) { %>
    <p>Your score: <%= score %> out of <%= totalQuestions %></p>
    <!-- Add personalized messages based on the score if needed -->
    <% if (score >= 8) { %>
      <p>Great job! You really know your stuff!</p>
    <% } else if (score >= 5) { %>
      <p>Not bad, but there's room for improvement.</p>
    <% } else { %>
      <p>Don't worry, practice makes perfect!</p>
    <% } %>
  <% } else { %>
    <p>Score not available.</p>
  <% } %>

  <!-- Include any additional content or feedback based on the score -->

  <!-- Include any JavaScript files -->
  <script src="/js/scripts.js"></script>
</body>
</html>
